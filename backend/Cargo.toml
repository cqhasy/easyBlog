[package]
name = "easyblog"
version = "0.1.0"
edition = "2021"

[lib]
name = "easyblog_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[[bin]]
name = "easyblog"
path = "src/main.rs"

[dependencies]
tauri = { version = "2", features = [] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
